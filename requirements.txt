# Versions pinned from the environment the notebooks were actually built
# and executed against (verified via nbclient, not interactively in a
# Jupyter server -- ipykernel is pinned/verified; notebook/jupyterlab are
# left unpinned since either works as the actual UI and neither was
# specifically tested here).
torch==2.8.0
numpy==1.22.4
scipy==1.12.0
pandas==1.5.3
matplotlib==3.5.3
pynapple==0.6.1
dill==0.4.1
ratinabox==1.7.1
ipykernel==6.31.0
notebook
